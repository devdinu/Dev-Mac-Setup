---
# - name: install nginx
  # apt: name=nginx state=latest
- name: install git
  apt: name=git state=latest force=yes cache_valid_time=3600

- name: copy git configuration
  template: src={{templates_path}}/.gitconfig dest={{remote_home}}/.gitconfig